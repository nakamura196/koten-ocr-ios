{
  "name": "koten-ocr-ios-site",
  "private": true,
  "type": "module",
  "description": "Checks for the KotenOCR landing page (docs/), served by Vercel at https://kotenocr.ldas.jp. The iOS app itself is built with Xcode.",
  "scripts": {
    "test": "node --test tests/*.test.mjs && node scripts/check-site.mjs",
    "test:site": "node scripts/check-site.mjs",
    "test:live": "node scripts/check-live.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
